/**
 * 이 게임의 이름과 한 줄 소개 — **여기 한 곳에서만 정한다.**
 *
 * 예전에는 같은 이름이 세 군데에 흩어져 있었다. 첫 화면의 큰 제목(Screens.ts),
 * 브라우저 탭 제목(index.html 의 `<title>`), 링크로 공유했을 때 보이는 설명
 * (`<meta name="description">`). 이름을 바꾸려면 세 곳을 다 찾아야 했고, 하나를
 * 빠뜨리면 탭 제목과 화면 제목이 서로 다른 이름을 말하게 된다.
 *
 * HTML 쪽은 `{{APP_NAME}}` 처럼 자리만 비워 두고 빌드할 때 이 값으로 채운다
 * (vite.config.ts). 화면을 그리고 나서 자바스크립트로 바꿔치기하지 않는 이유는,
 * 그러면 링크 미리보기를 만드는 쪽(자바스크립트를 돌리지 않는다)이 빈 제목을 읽기
 * 때문이다.
 */

/**
 * 이름 — **조각으로 적는다.**
 *
 * **`AI 안전운전 교실 : 우회전과 어린이보호구역 일시정지 연습편`**
 *
 * **제목은 여기가 무엇 하는 곳인지**를 말한다 — 안전운전을 배우는 교실. **부제는 이번 편이 무엇을 다루는지**를
 * 말한다 — 우회전과 어린이보호구역에서의 일시정지. 제목과 부제가 이렇게 갈리면, 나중에 다른 주제를 다루게 돼도
 * ('좌회전 편' · '고속도로 편') **제목은 그대로 두고 부제만 바꾸면 된다.**
 *
 * '연습편' 의 '편' 이 그 자리를 미리 열어 둔다. 사용자가 이 꼴을 골랐다 (후보 중 "이게 제일 좋겠다").
 *
 * 화면의 호칭('안전운전 Level6' · '안전운전 마스터' — curriculum.ts 의 courseTitle)과도 **같은 낱말**이라,
 * 첫 화면부터 결과 화면까지 부르는 말이 하나로 통한다.
 *
 * ## 왜 '우회전' 이 이름에서 빠졌는가
 *
 * 오래 '우회전' 을 이름에 두었다 — 이 게임이 다루는 것의 알맹이라서다. 그런데 **같은 낱말을 쓴 다른 작품이
 * 먼저 알려지면서**, 이름에 그 낱말을 두는 것만으로 따라 한 것처럼 보이게 됐다 (사용자가 짚었다).
 * 그래서 제목에서는 빼고 **부제에 남긴다** — 무엇을 다루는지는 부제가 말하고, 검색도 부제가 받는다.
 *
 * 학습자의 호칭도 이름을 따라 **'안전운전 Level6' · '안전운전 마스터'** 가 됐다 (curriculum.ts 의 courseTitle · 사용자 요청) —
 * 오래 '우회전 Level6' 이었는데, 부르는 말과 이름이 서로 다른 것을 가리키면 레벨이 무엇의 레벨인지부터 다시 알아봐야 한다.
 * 다만 호칭은 이름이 아니라 **키우는 능력의 이름**이라 **딱지로 칠하지 않는다** (ui/brandName.ts).
 *
 * **이름을 정할 때는 먼저 검색해 겹치는 것이 있는지 본다.** '우회전 참교육' 은 그러지 않아 다른 작품과 겹쳤고,
 * 이름 · 로고 · 장표를 다시 만드는 일이 됐다.
 *
 * ## 왜 조각인가
 *
 * 글자마다 **서로 다른 색**을 준다 (아래 `tone`). 완성된 이름에서 글자를 찾아 자르지 않는 이유는, 같은 글자가
 * 뒤에도 또 나올 수 있어 찾아 자르면 이름을 바꿨을 때 엉뚱한 곳이 잘리기 때문이다. **조각을 먼저 정하고 이름을
 * 그 조각들로 만든다.**
 *
 * **길어지면 첫 화면의 히어로가 접힌다.** 이 값을 바꿀 때는 좁은 화면(390px)에서 어떻게 접히는지 보고 정한다 —
 * 히어로는 이름과 탭뿐인 자리라 이름이 흔들리면 그 화면에서 가장 큰 덩어리가 흔들린다.
 */

/**
 * 이름 한 조각.
 *
 * `tone` 이 붙은 조각만 색을 입는다 (index.html 의 `.brand-yellow` · `.brand-stop` · `.brand-ai`).
 * 색을 여기서 정하지 않고 이름만 주는 이유는, 값은 화면의 것이고 **여기서 정하는 것은
 * "이 글자가 신호등의 어느 등인가"** 이기 때문이다.
 */
export interface NamePart {
  text: string;
  /**
   * 신호등의 세 등 — 적색 · 황색 · 녹색. `green` 은 제목의 '안전운전', `yellow` 는 부제의 '우회전' ·
   * '어린이보호구역'(주의 표지처럼 노란 바탕 · 어두운 글자)이 쓴다. `red` 는 지금 쓰는 곳이 없다. `ai` 는 'AI' 배지 (신호등과 다른 모양 · 다른 색).
   * `stop` 은 **제목의 정지 표지** — 붉은 바탕의 '일시정지'.
   */
  tone?: 'red' | 'yellow' | 'green' | 'ai' | 'stop';
  /**
   * 줄이 접혀도 **한 덩어리로** 내려간다. 좁은 화면(390px)에서 부제가 두 줄이 될 때 마지막 낱말만 홀로 아랫줄에
   * 남았다 — 딱지의 여백만큼 윗줄이 길어져서다. 마지막 낱말을 묶어 두면 '… 일시정지' 에서 끊긴다.
   */
  keep?: boolean;
}

/**
 * 이름의 조각들 — **'안전운전' 이 녹색 딱지다.**
 *
 * 신호의 녹색이다. 제목의 녹색 하나와 부제의 노랑 둘 · 빨강 하나가 모이면 **이름 전체가 신호등 세 색**으로
 * 선다 — 녹색은 가도 좋은 것(안전운전), 노랑은 조심할 자리(우회전 · 어린이보호구역), 빨강은 서야 하는 것(일시정지).
 *
 * 예전 이름('AI 우회전 참교육')은 '우회전' 세 글자가 그 세 등을 나눠 가졌다. 이름이 바뀌며 색은 남고
 * 맡는 낱말만 달라졌다.
 *
 * **`AI` 는 배지다** — AI 활용 공모전에 내는 작품이라 AI 가 돋보여야 한다(사용자가 "AI 가 너무 돋보이지 않아" 라고
 * 짚었다). 다만 표지 딱지와 섞이면 표지 하나가 더 선 것처럼 읽히므로 **모양도 색도 다르게** 둔다 — 둥근 알약 모양에,
 * 표지에 없는 파랑→보라 그라데이션과 은은한 빛, 흔히 AI 를 뜻하는 반짝이(✦). 색은 index.html 의 `.brand-ai` 가 정한다.
 *
 * `교실` 에는 색을 주지 않는다. 배지 하나와 딱지 한 장이 이미 주인공이라, 더 칠하면 무엇이 이름인지 흐려진다.
 */
export const APP_NAME_PARTS: readonly NamePart[] = [
  { text: 'AI', tone: 'ai' },
  { text: ' ' },
  { text: '안전운전', tone: 'green' },
  { text: ' 교실' },
];

export const APP_NAME = APP_NAME_PARTS.map((p) => p.text).join('');

/**
 * **브라우저 탭 아이콘** — 붉은 **팔각형**에 흰 '정' 한 글자.
 *
 * 16px 짜리 탭 아이콘에 이름을 다 넣으면 뭉개져 아무것도 읽히지 않으므로 한 글자만 남긴다. 그 한 글자를
 * '일시정지' 의 '정' 으로 두고, 바탕을 **정지 표지의 붉은 팔각형**으로 그린다 — 글자가 안 읽히는 크기에서도
 * **모양만으로 '서라' 가 읽힌다.** (예전 이름일 때는 주황 네모에 흰 '우' 였다. 이름에서 '우회전' 이 빠지며 함께 바꿨다.)
 *
 * **그림 파일이 아니라 SVG 다.** 크기마다 흐려지지 않고(탭 16 · 즐겨찾기 32 · 홈 화면 180), 몇백 바이트라
 * 단일 파일 빌드에 통째로 심어도 부담이 없다 (vite.config.ts 가 data URI 로 넣는다). 레벨 뱃지를 그림에서
 * SVG 로 옮긴 것과 같은 이유다 (ui/badges.ts).
 *
 * 글꼴은 **시스템 것을 쓴다.** 한글 글꼴을 파일로 실어 아이콘에 넣을 수는 없어서, 한국어를 읽는 기기라면
 * 있게 마련인 글꼴을 차례로 적어 둔다.
 */
export const APP_ICON_SVG = [
  `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">`,
  // 정지 표지의 팔각형 — 64칸 안에 꽉 차게 앉히고, 모서리를 살짝 둥글려 탭에서 거칠어 보이지 않게 한다
  `<path d="M20.7 2h22.6L62 20.7v22.6L43.3 62H20.7L2 43.3V20.7Z" fill="#c1272d" stroke="#ffffff" stroke-width="3" stroke-linejoin="round"/>`,
  `<text x="32" y="34" text-anchor="middle" dominant-baseline="central"`,
  ` font-family="Pretendard,'Apple SD Gothic Neo','Malgun Gothic','Noto Sans KR',sans-serif"`,
  ` font-size="34" font-weight="800" fill="#ffffff">정</text>`,
  `</svg>`,
].join('');

/**
 * 이름 옆에 붙는 한마디. 브라우저 탭에 `이름 — 부제` 꼴로 들어간다.
 *
 * 무엇을 다루는지(우회전과 어린이보호구역)를 이름보다 넓게 풀어 준다 — 탭 제목과 검색 결과에서 읽는 줄이다.
 *
 * **부제가 '이번 편' 이다.** 제목(안전운전 교실)이 여기가 무엇 하는 곳인지 말하고, 부제가 지금 다루는 주제를
 * 말한다 — 우회전과 어린이보호구역의 일시정지. 다른 주제를 더하게 되면 이 줄만 바꾼다.
 *
 * **딱지 셋이 곧 이 작품의 세 낱말이다** (사용자가 색까지 정했다) — **'우회전' 과 '어린이보호구역' 은 노란
 * 바탕**(주의 표지처럼 어두운 글자), **'일시정지' 는 정지 표지의 붉은 바탕**. 노랑 둘은 **어디서**,
 * 빨강 하나는 **무엇을 해야 하는지**다 — 색만 훑어도 이 작품이 무엇을 시키는지 읽힌다. 나머지 낱말
 * (과 · 연습편)에는 칠하지 않는다 — 다 칠하면 무엇이 주제인지 흐려진다.
 *
 * **이름에서 빠진 '우회전' 이 여기 있다** — 제목에 두면 같은 낱말을 쓴 다른 작품과 겹쳐 보이지만, 부제에서는
 * 이번 편이 다루는 주제일 뿐이다. 검색도 이 줄이 받는다.
 */
export const APP_TAGLINE_PARTS: readonly NamePart[] = [
  { text: '우회전', tone: 'yellow' },
  { text: '과 ' },
  { text: '어린이보호구역', tone: 'yellow' },
  { text: ' ' },
  { text: '일시정지', tone: 'stop' },
  { text: ' ' },
  { text: '연습편', keep: true },
];

export const APP_TAGLINE = APP_TAGLINE_PARTS.map((p) => p.text).join('');

/**
 * **만든 사람** — 첫 화면 맨 아래의 저작권 줄과 About 창이 같이 쓴다 (Screens.ts 의 siteFooter · renderAbout).
 * 한 곳에 두어야 연락처를 바꿀 때 두 자리가 어긋나지 않는다.
 */
export const APP_AUTHOR = 'goodpjw2008';
export const APP_CONTACT = 'goodpjw2008@gmail.com';
/** 저작권 표기의 해 — 처음 만든 해다 (해마다 올리는 값이 아니다) */
export const APP_COPYRIGHT_YEAR = 2026;
/** 이용 조건 한 줄 — 차량 모델에 비상업(NC) 조건이 있어 이 작품 전체가 비영리다 (README '이미지 라이선스') */
export const APP_USAGE = '비영리 목적으로만 사용합니다';

/** 링크로 공유했을 때 제목 아래 붙는 설명 */
export const APP_DESCRIPTION =
  '2023년 개정 도로교통법의 우회전·어린이보호구역 규정을 3D 운전 시뮬레이션으로 익히는 학습용 게임';
